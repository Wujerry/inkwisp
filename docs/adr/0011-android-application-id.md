# Use com.inkwisp.app as the Android application ID

InkWisp will use `com.inkwisp.app` as its Android application ID, subject to final reservation in Google Play Console before the first upload. The identifier is deliberately brand-based rather than tied to a person or current publisher because it becomes effectively permanent once the application is published.
