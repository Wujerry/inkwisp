# Provide a managed default workspace

InkWisp will open into a zero-setup Managed Workspace and create documents there immediately; connecting an Android filesystem folder remains an optional advanced path. This deliberately trades strict user-selected-folder ownership for a much lower first-use burden, while keeping export and Connected Folder editing available so users are not locked into the managed library.
